import { Nav } from "@/components/Nav";
import HeroSection from "@/components/HeroSection";

import React from "react";

const page = () => {
  return (
    <section>
      <Nav />
      <HeroSection />
    </section>
  );
};

export default page;
